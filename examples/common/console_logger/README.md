# Console logger

This example writes informational and warning messages with deterministic
formatting options. The logger serializes its console output across threads.

## Run

Run `common_console_logger`. It prints one `INF` line and one `WRN` line.

