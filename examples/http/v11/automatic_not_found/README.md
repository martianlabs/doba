# Automatic not found

This server has no routes. A request to an origin-form target therefore receives
the server's automatic `404 Not Found` response.

## Try it

```text
curl -i http://localhost:8080/missing
```

