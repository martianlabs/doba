cmake_minimum_required(VERSION 3.20)
project(request_body_text LANGUAGES CXX)
add_executable(request_body_text "${CMAKE_CURRENT_LIST_DIR}/main.cpp")
target_link_libraries(request_body_text PRIVATE doba_headers)
source_group(TREE "${CMAKE_CURRENT_LIST_DIR}" FILES "${CMAKE_CURRENT_LIST_DIR}/main.cpp")
